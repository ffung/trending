version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
  pre_build:
    commands:
      - pip install -r trending/requirements.txt
  build:
    commands:
      - python -m pytest
artifacts:
  files:
    - '**/*'
  base-directory: trending
